<odoo>
    <data>

        <record id="vehicle_sale_action_quotations" model="ir.actions.act_window">
            <field name="name">Quotations</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.order</field>
            <field name="view_type">form</field>
            <field name="view_id" ref="sale.view_quotation_tree"/>
            <field name="domain">[('sale_aftersales','=', 'sales'),('sale_type','=','vehicle')]</field>
            <field name="context">{'default_sale_type': 'vehicle','default_sale_aftersales':'sales'}</field>
            <field name="view_mode">tree,kanban,form,calendar,pivot,graph</field>
            <field name="search_view_id" ref="sale.sale_order_view_search_inherit_quotation"/>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Create a Quotation, the first step of a new sale.
                </p>
                <p>
                    Your next actions should flow efficiently: confirm the Quotation
                    to a Sales Order, then create the Invoice and collect the Payment.
                </p>
            </field>
        </record>

        <record id="vehicle_sale_action_orders" model="ir.actions.act_window">
            <field name="name">Sales Orders</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.order</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,kanban,form,calendar,pivot,graph</field>
            <field name="search_view_id" ref="sale.sale_order_view_search_inherit_sale"/>
            <field name="context">{'default_sale_type': 'vehicle','default_sale_aftersales':'sales'}</field>
            <field name="domain">[('state', 'not in', ('draft', 'sent', 'cancel')),
                                ('sale_aftersales','=', 'sales'),('sale_type','=','vehicle')]
            </field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Create a Quotation, the first step of a new sale.
                </p>
                <p>
                    Once the quotation is confirmed, it becomes a sales order.
                    You'll be able to invoice it and collect payments.
                    From the <i>Sales Orders</i> menu, you can track delivery
                    orders or services.
                </p>
            </field>
        </record>

        <record id="vehicle_sale_action_orders_to_invoice" model="ir.actions.act_window">
            <field name="name">Orders to Invoice</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.order</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form,calendar,graph,pivot,kanban</field>
            <field name="context">{'create': False,'default_sale_type': 'vehicle'}</field>
            <field name="domain">[('invoice_status','=','to invoice'),('sale_aftersales','=', 'sales'),('sale_type','=','vehicle')]</field>
            <field name="search_view_id" ref="sale.view_sales_order_filter"/>
            <field name="help" type="html">
                <p>
                    You will find here all orders that are ready to be invoiced.
                </p>
                <p>
                    You can select all orders and invoice them in batch, or check
                    every order and invoice them one by one.
                </p>
            </field>
        </record>

        <record id="vehicle_sale_action_orders_upselling" model="ir.actions.act_window">
            <field name="name">Orders to Upsell</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.order</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form,calendar,graph,pivot,kanban</field>
            <field name="domain">[('invoice_status','=','upselling'),('sale_aftersales','=', 'sales'),('sale_type','=','vehicle')]</field>
            <field name="context">{'create': False,'default_sale_type': 'vehicle','default_sale_aftersales':'sales'}</field>
            <field name="search_view_id" ref="sale.view_sales_order_filter"/>
            <field name="help" type="html">
                <p>
                    Orders to upsell are orders having products with an invoicing
                    policy based on <i>ordered quantities</i> for which you have
                    delivered more than what have been ordered.
                </p>
                <p>
                    <i>Example: pre-paid service offers for which the customer have
                        to buy an extra pack of hours, because he used all his support
                        hours.
                    </i>
                </p>
            </field>
        </record>

        <record id="ac_vehicle_management_action_order_report_all" model="ir.actions.act_window">
            <field name="name">Vehicle Sales Analysis</field>
            <field name="res_model">sale.report</field>
            <field name="view_type">form</field>
            <field name="view_mode">graph,pivot</field>
            <field name="view_id"></field>  <!-- force empty -->
            <field name="search_view_id" ref="sale.view_order_product_search"/>
            <field name="context">{'search_default_Sales':1, 'group_by_no_leaf':1,'group_by':[]}</field>
            <field name="domain">[('sale_aftersales','=', 'sales'),('sale_type','=','vehicle')]</field>
            <field name="help">This report performs analysis on your quotations and sales orders. Analysis check your
                sales revenues and sort it by different group criteria (salesman, partner, product, etc.) Use this
                report to perform analysis on sales not having invoiced yet. If you want to analyse your turnover, you
                should use the Invoice Analysis report in the Accounting application.
            </field>
        </record>


    </data>
</odoo>
