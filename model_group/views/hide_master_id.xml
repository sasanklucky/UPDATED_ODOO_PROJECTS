<odoo>
    <data>
        <!-- Inherit form view to hide create/edit buttons for restricted users -->
        <record id="view_model_groups_form_inherit_buttons" model="ir.ui.view">
            <field name="name">model.groups.form.restrict.buttons</field>
            <field name="model">model.groups</field>
            <field name="inherit_id" ref="model_group.view_model_groups_form"/> <!-- Replace if custom view ID -->
            <field name="groups_id" eval="[(4, ref('model_group.group_model_group_security'))]"/>
            <field name="arch" type="xml">
                <form position="attributes">
                    <attribute name="create">false</attribute>
                    <attribute name="edit">false</attribute>
                </form>
            </field>
        </record>
    </data>
</odoo>
