<odoo>
    <data>

        <record id="view_res_partners_form_ars_inherited" model="ir.ui.view">
            <field name="name">view.res.partner.form.ars.inherited1</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field eval="18" name="priority"/>
            <field name="groups_id" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
            <field name="arch" type="xml">
                <data>
                    <button name="toggle_active" position="before">
                        <button type="object" icon="fa-car" name="customer_estimate">
                            <field  string="Estimate" name="estimate_count" widget="statinfo"/>
                        </button>
                        <button  type="object" icon="fa-car" name="customer_so_preparation">
                            <field  string="SO Preparation" name="so_count" widget="statinfo"/>
                        </button>
                        <button  type="object" icon="fa-car" name="customer_service_order">
                            <field  string="Service Order" name="order_count" widget="statinfo"/>
                        </button>
                    </button>
                </data>
            </field>
        </record>

        <!--<record id="view_action_split_form" model="ir.ui.view">
            <field name="name">view.action.split.form</field>
            <field name="model">split.lineitem</field>
            <field name="arch" type="xml">
                <form>
                    <group>
                        <div>
                            <template id=""/>
                        </div>
                    </group>
                </form>
            </field>
        </record>

        <record id="action_split_invoice_form" model="ir.actions.act_window">
            <field name="name">Split Invoice</field>
            <field name="res_model">split.lineitem</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,kanban,form</field>
            <field name="view_id" ref="view_action_split_form"/>
        </record>

        <act_window name="Split Invoice"
            res_model="split.lineitem"
            src_model="sale.order"
            key2="client_action_multi"
            view_mode="form" target="new"
            view_type="form"
            id="action_split_invoice_form" />-->


    </data>
</odoo>