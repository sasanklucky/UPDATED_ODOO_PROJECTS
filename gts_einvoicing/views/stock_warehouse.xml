<?xml version="1.0" encoding="UTF-8"?>

<odoo>
    <record id="form_view_stock_warehouse_inherit1" model="ir.ui.view">
        <field name="name">stock.warehouse.form.inhert</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='code']" position="after">
                <field name="configure_einvoice"/>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="E-Invoicing Bill Credentials" attrs="{'invisible': [('configure_einvoice','=',False)]}">
                    <group>
                        <group string="Locations">
                            <field name="user_name"/>
                            <field name="gst_no"/>
                            <field name="user_password"/>
                        </group>
                        <group>
                            <field name="auth_token"/>
                            <field name="expire_date"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
